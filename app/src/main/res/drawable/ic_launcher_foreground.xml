<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Adaptive icons reserve the outer ~18dp for masking, so the artwork stays
         inside the middle 72dp safe zone. -->
    <path
        android:fillColor="#FF32D74B"
        android:pathData="M54,30 L72,76 L54,66 L36,76 Z" />
</vector>
